{
  "name": "trello-label-sync-action",
  "version": "0.2.5",
  "description": "A github action to sync trello labels with github branches and pull requests",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "prepare": "ncc build index.js -o dist --source-map --license licenses.txt",
    "test": "jest",
    "all": "npm run lint && npm run prepare && npm run test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kevinthompson/trello-label-sync-action.git"
  },
  "keywords": [
    "Trello",
    "GitHub",
    "Actions",
    "JavaScript"
  ],
  "author": "Kevin Thompson",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.6.0",
    "@actions/github": "^5.0.0",
    "node-fetch": "^3.1.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.33.0",
    "eslint": "^8.4.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^4.0.0",
    "jest": "^27.4.3",
    "prettier": "^2.5.1"
  }
}
